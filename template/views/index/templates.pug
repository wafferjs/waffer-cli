template#template-index
  .main
    h1 
      | \
      span v
      | /

    h2 waffer
      span view: #{view}
      
    view-component

template#template-404
  .main
    pre
      | // 404 page not found.
      | if (!page) {
      |   throw new Error("(╯°□°)╯︵ ┻━┻");
      | } 
